#/bin/bash

DMENU='dmenu -fn 'TerminessTTF-11' -i -l 10 -x 205 -w 950 -y 270 -nb #000000 -nf #999999 -sb #FFFFFF -sf #31658C'
choice=$(echo -e "light\nwindows\nxp\nkali\nlinux-white\nlinux-blue\nmac\nmecha-buckling-on\nmecha-gateronred-on\nmecha-mxblue-on\nmecha-mxblue-soft-on\nmecha-mxbrown-on\nmecha-mxreds-on\nmecha-off\nbar-on\nbar-off\nkeyboard\nmousepad\ndark\nwide\nnormal\nlock\nlogout\nviter\nnetwork\nreboot\nshutdown\nsuspend\nterminal\nkonsol\nrefresh\nlist\nfilemanager\nplay\naria\nwallpaper\ncalc\ntypora\ntag\ngammy\ntouchpad\nconvert\nfile\nscriptedit\nbuku\nrecord\nstop\nmonitor\nkill\ndockon\ndockoff\nvim\nsteam\ntvs\npron\nvideo\ndrawing\nfceux\nkalkulator\ntelegram\nqutebrowser\ncadence\nolive\nfilm\npavucontrol\nmute\nvolmax\nvolhalf\nnes\nsnes\nbright30\nbright50\nbright100\nplayer\nmusic\npause\nstoprec\nnext\nprev\nminimize\nmaximize\nfullscreen\nmpvcontrol\nclose\nscreenshot\nscreenshotarea\nscreenshotdelay\nyoutube\ncompton\nxeyes\nvokoscreen\nudiskie" | $DMENU)

case "$choice" in
      light) wal -f ~/Documents/colorscheme/3024.light.txt ;;
      keyboard) onboard  ;;
      mousepad) mousepad  ;;
      windows) ~/Documents/scripts/windows  ;;
      xp) ~/Documents/scripts/windowsxp  ;;
      kali) ~/Documents/scripts/kali  ;;
      linux-white) ~/Documents/scripts/linux-white  ;;
      linux-blue) ~/Documents/scripts/linux-blue  ;;
      mac) ~/Documents/scripts/mac  ;;
      mecha-buckling-on) buckle -g 50  ;;
      mecha-gateronred-on) buckle -g 50 -s 530 -p ~/Documents/keyboardsounds/gateronreds ;;
      mecha-mxblue-on) buckle -g 50 -s 50 -p ~/Documents/keyboardsounds/mxblues ;;
      mecha-mxblue-soft-on) buckle -g 50 -s 50 -p ~/Documents/keyboardsounds/mxbluesofts ;;
      mecha-mxbrown-on) buckle -g 50 -s 50 -p ~/Documents/keyboardsounds/mxbrowns ;;
      mecha-mxreds-on) buckle -g 50 -s 50 -p ~/Documents/keyboardsounds/mxreds ;;
      mecha-off) pkill buckle  ;;
      bar-on) ~/Documents/scripts/polyon  ;;
      bar-off) ~/Documents/scripts/polyoff  ;;
      light) wal -f ~/Documents/colorscheme/3024.light.txt ;;
      wide) xrandr --output eDP1 --scale-from 1440x900 --panning 1440x900 ;;
      normal) xrandr --output eDP1 --scale-from 1366x768 --panning 1366x768 ;;
      dark) wal -f ~/Documents/colorscheme/solarized.dark.txt ;;
      refresh) i3-msg restart ;;
      list) ~/Documents/scripts/list ;;
      wallpaper) ~/Documents/scripts/gantifill ;;
      viter) python3 ~/Documents/bangunapp/viter/viter.py ;;
      calc) libreoffice --calc ;;
      typora) typora ;;
      tag) easytag ;;
      gammy) gammy ;;
      terminal) xterm ;;
      konsol) ppsspp ;;
      filemanager) pcmanfm ;;
      player) smplayer ;;
      music) ~/Documents/scripts/mpds ;;
      aria) ~/Documents/scripts/dmenu_aria2 ;;
      touchpad) ~/.config/i3/touchpad.sh devonoff ;;
      convert) ~/Documents/scripts/converthb ;;
      file) ~/Documents/scripts/dmenufm ;;
      scriptedit) ~/Documents/scripts/confmenu ;;
      buku) ~/Documents/scripts/books ;;
      record) ~/Documents/scripts/dmenurecord ;;
      stoprec) ~/Documents/scripts/stoprec ;;
      monitor) ~/Documents/scripts/dmenu_display ;;
      kill) ~/Documents/scripts/dmenu-kill-process ;;
      dockon) ~/Documents/scripts/killdock on >/dev/null 2>&1 & ;;
      dockoff) ~/Documents/scripts/killdock off >/dev/null 2>&1 & ;;
      vim) xterm -e vim ;;
      steam) steam ;;
      tvs)~/Documents/scripts/tvs ;;
      pron)~/Documents/scripts/sans ;;
      video)~/Documents/scripts/mpvinfinite ;;
      drawing) drawing ;;
      fceux) ~/Documents/scripts/emu ;;
      kalkulator) ~/Documents/scripts/itung ;;
      telegram) nice -n19 telegram-desktop ;;
      qutebrowser) qutebrowser --backend webengine ;;
      cadence) cadence ;;
      olive) olive-editor ;;
      film) ~/Documents/scripts/film ;;
      pavucontrol) pavucontrol-qt ;;
      mute) pactl set-sink-mute 0 toggle ;;
      volmax) pamixer --set-volume 100 ;;
      volhalf) pamixer --set-volume 50 ;;
      nes) fceux ;;
      snes) snes9x-gtk ;;
      bright30) light -S 30 ;;
      bright50) light -S 50 ;;
      bright100)light -S 100 ;;
      play) ~/Documents/scripts/mpds ;;
      pause) mpc toggle ;;
      stop) mpc stop ;;
      next) mpc next ;;
      prev) mpc prev ;;
      minimize) i3-msg move scratchpad ;;
      maximize) ~/Documents/scripts/hide ;;
      close) i3-msg kill & ;;
      fullscreen) i3-msg fullscreen ;;
      mpvcontrol) echo cycle pause | socat - ~/.config/mpv/socket ;;
      screenshot) ~/Documents/scripts/screenshot.sh & ;;
      screenshotarea) ~/Documents/scripts/screenshot.sh -s & ;;
      screenshotdelay) ~/Documents/scripts/scrots & ;;
      youtube) ~/Documents/scripts/ytplay & ;;
      compton) ~/Documents/scripts/compton-toggle.sh & ;;
      vokoscreen) vokoscreen & ;;
      xeyes) xeyes -fg black & ;;
      udiskie) ~/Documents/scripts/udiskie-dmenu & ;;
      lock) betterlockscreen -l & ;;
      network) networkmanager_dmenu  & ;;
      logout) i3-msg exit & ;;
      reboot) exec systemctl reboot & ;;
      shutdown) exec systemctl -i poweroff & ;;
      suspend) exec systemctl suspend & ;;
esac

